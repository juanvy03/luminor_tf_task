# version: 3
version: 3

workflows:
  test:
    plan:
      steps:
      - init:
          extra_args: ["-var-file=luminor_eks.tfvars"]
      - plan:
          extra_args: ["-var-file=luminor_eks.tfvars"]
    apply:
      steps:
      - apply:
          extra_args: ["-var-file=luminor_eks.tfvars"]