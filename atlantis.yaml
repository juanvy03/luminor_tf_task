# version: 3
version: 3

projects:
- name: luminor-test
  dir: .
  workflow: luminor

workflows:
  luminor:
    plan:
      steps:
      - init
      - plan:
          extra_args: ["-var-file","luminor_eks.tfvars"]
    apply:
      steps:
      - apply:
          extra_args: ["-var-file","luminor_eks.tfvars"]