# version: 3
version: 3

projects:
- name: luminor-test
  dir: luminor_tf_task
  workflow: test

workflows:
  test:
    plan:
      steps:
      - init
      - run: ["terraform","plan","-var-file","luminor_eks.tfvars"]
    apply:
      steps:
      - run: ["terraform","apply","-var-file","luminor_eks.tfvars"]
          