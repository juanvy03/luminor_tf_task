# version: 3
version: 3

projects:
- name: luminor-test
  dir: luminor_tf_task
  workflow: test

workflows:
  test:
    plan:
      steps:
      - init
      - plan:
          extra_args: ["-var-file","luminor_eks.tfvars"]
    apply:
      steps:
      - apply:
          extra_args: ["-var-file","luminor_eks.tfvars"]